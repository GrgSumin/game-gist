# Backend API base URL.
# Leave empty in development to use the Vite proxy defined in vite.config.ts.
# Set to the full backend origin (no trailing slash, no /api suffix) in production.
# Example: https://api.gamegist.com
VITE_API_URL=
